# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo

# Exe build artifacts
*.exe
*.cjs
*.bat
*.blob
sea-prep.blob
server-bundled.cjs
ed-colony-tracker.bat

# Runtime data (colony-data*.json also covers timestamped recovery snapshots)
colony-data*.json
colony-token.txt
colony-images/
# Mining side-stores: append-only rock log + cached ring/hotspot index. Deliberately outside
# colony-data.json so the synced state blob doesn't grow without bound.
mining-log.jsonl
mining-rings.json
mining-trophies.json
journal-stats.json
chain-watch.json
mining-annotations.json
colony-gallery.json
backups/
backups - Copy/
app-icon.ico

# Co-pilot runtime state — personal data (commander/crew names, Q&A answers about the user,
# colony coords, every generated line + its inputs). Written to APP_DIR (repo root in dev).
copilot-memory.json
copilot-captures.jsonl
copilot-characters/

# Project handoff
PROJECT-HANDOFF.md

# Claude
.claude/

# Sample journal data from friend (not part of repo)
Buz/
